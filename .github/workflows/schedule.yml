name: Schedule
on:
  workflow_dispatch:
  schedule:
    - cron: '*/3 * * * *'

jobs:
  run-scheduled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow runs every 3 minutes."
      - run: date
